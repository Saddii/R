
Raport prezentuje analizę metod klasyfikacji na podstawie danych związanych z irysami oraz szkłem. Pierwsza część raportu skupia się na metodach klasyfikacji, takich jak regresja liniowa, algorytm k najbliższych sąsiadów (KNN), drzewa klasyfikacyjne i naiwny klasyfikator bayesowski. Następnie te metody są porównywane na przykładowych danych.

Wstęp:
Raport prezentuje metody klasyfikacji, takie jak regresja liniowa, KNN, drzewa klasyfikacyjne i naiwny klasyfikator bayesowski. Metody te zostaną porównane na próbkach danych.

Klasyfikacja na bazie modelu regresji liniowej:
W tej sekcji danych użyto do przeprowadzenia regresji liniowej na danych dotyczących irysów. Użyto różnych zbiorów uczących i testowych do obliczenia błędów klasyfikacji na obu zbiorach.

Model regresji dla składników wielomianowych stopnia 2:
Został zastosowany model regresji dla składników wielomianowych stopnia 2, aby zbadać dokładność klasyfikacji. Błędy klasyfikacji na zbiorze uczącym i testowym zostały obliczone i porównane z modelem liniowym.

Zadanie2:
Przeprowadzono analizę na zestawie danych dotyczących szkła. Skonstruowano modele klasyfikacji, takie jak KNN, drzewa klasyfikacyjne i naiwny klasyfikator bayesowski, aby ocenić ich skuteczność w klasyfikacji różnych typów szkła.

Testowanie metod dla różnych parametrów oraz podzbioru cech:
Przeprowadzono testowanie metody KNN dla różnych parametrów, takich jak ilość sąsiadów. Błędy klasyfikacji zostały ocenione za pomocą walidacji krzyżowej oraz metody bootstrap, a wyniki zostały porównane.
